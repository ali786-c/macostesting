'use client';

/**
 * Pull-to-refresh désactivé sur toutes les pages (mobile + app) pour éviter
 * les reloads intempestifs et les conflits avec les listes/drawers.
 * Le composant reste dans le layout mais ne fait rien.
 */
export default function PullToRefresh() {
  return null;
}
